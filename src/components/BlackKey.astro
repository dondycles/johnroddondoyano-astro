---
import { cn } from '@/lib/utils';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div
  class={cn(
    `bg-foreground hover active absolute left-0 -bottom-4.5 z-50 h-9 w-[65%]  shadow`,
    className
  )}
  aria-hidden="true"
>
</div>
